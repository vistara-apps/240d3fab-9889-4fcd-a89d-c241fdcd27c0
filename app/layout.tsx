import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';
import { ThemeProvider } from './components/ThemeProvider';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'TradeSage - Automate your trades, master your psychology',
  description: 'A tool for crypto traders to automate order placement, minimize slippage, journal trades with emotional context, and track rule adherence.',
  keywords: ['crypto', 'trading', 'automation', 'psychology', 'base', 'miniapp'],
  authors: [{ name: 'TradeSage Team' }],
  viewport: 'width=device-width, initial-scale=1',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider>
          <Providers>
            {children}
          </Providers>
        </ThemeProvider>
      </body>
    </html>
  );
}
